def check_order(string, pattern):
    j = 0
    for char in string:
        if char == pattern[j]:
            j += 1
        
        if j == len(pattern):
            return True
            
    return False

string = 'engineers rock'
pattern = 'nik'
print(check_order(string, pattern))
