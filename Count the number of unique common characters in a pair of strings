def matching_characters(str1, str2):
    common=set({})
    for i in str1:
        for j in str2:
            if i == j:
                common.add(j)
            
    print(len(common))
    
str1 = 'abcdef'
str2 = 'defghiaaa'
matching_characters(str1, str2)
